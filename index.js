function highlight() {
  const text = document.getElementById("textInput").value;
  const search = document.getElementById("searchInput").value;
  
  const highlight = new RegExp(search, 'hae')
  const hightlightTexr = text.replace
}
